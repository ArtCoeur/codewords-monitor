/**
 * Handle all facts
 *
 * @param fact
 */
module.exports.newFact = function(pub, fact) {
    switch (fact.name){
        case 'board.new':
            break;
        case 'word.new':
            break;
        case 'cell.updated':
            break;
        default :
            //

    }
};